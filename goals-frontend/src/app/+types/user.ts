import React from 'react';

export interface User {
  username: string;
  password: string;

  // descriptionParagraph: string;

  // tags: Object[]; // Array of references to tags
  // goals: Object[]; // Array of refrences to goals
}